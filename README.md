# React + TypeScript + Vite + tailwind with ( shadcn )

Required Node version >= 20

- Install node via nvm [NVM Installation guide](https://github.com/nvm-sh/nvm)

```
npm i
```

```
npm run dev
```
